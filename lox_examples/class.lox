
class Vector {
	init(x, y) {
		this.x = x;
		this.y = y;
	}
	add(v) {
		return Vector(this.x + v.x, this.y + v.y);
	}
	string() {
		return "<" + this.x + ", " + this.y + ">";
	}
}

var v1 = Vector(1, 2);
var v2 = Vector(3, 4);
print v1.string();
print v2.string();
print v1.add(v2).string();
